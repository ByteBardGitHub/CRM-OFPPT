import NavbarSidebarLayout from "../layouts/navbar-sidebar";
import type { FC } from "react";

const PaiementPage: FC = function () {
  return (
    <NavbarSidebarLayout>
      <div>Paiement Page</div>
    </NavbarSidebarLayout>
  );
};

export default PaiementPage;
